{
  "config": {
    "abort": {
      "already_configured": "Account is already configured.",
      "single_instance_allowed": "Only a single configuration is allowed."
    },
    "error": {
      "domain_not_found": "The specified domain was not found in your account.",
      "invalid_api_key": "The provided API key is invalid.",
      "unauthorized": "The Account Update Token is invalid.",
      "retry_limit_exceeded": "Failed to connect to IPv64.net after multiple attempts. Please check your network."
    },
    "step": {
      "user": {
        "data": {
          "api_key_economy": "Enable economy mode (updates only when IP changes)",
          "apikey": "API Key",
          "domain": "Domain",
          "scan_interval": "Update interval (0-60 minutes, 0=disabled)",
          "token": "Account Update Token"
        },
        "description": "IPv64.net provides DynDNS and healthcheck services, allowing you to manage domains and keep devices accessible despite changing IP addresses. Enter your API key and Account Update Token to set up the integration.",
        "title": "IPv64.net"
      }
    },
  "options": {
    "step": {
      "init": {
        "data": {
          "api_key_economy": "Enable economy mode (updates only when IP changes)",
          "scan_interval": "Update interval (0-60 minutes, 0=disabled)"
        },
        "description": "Configure the update interval and economy mode. Free accounts have 64 updates per day. Recommended interval: 23 minutes (24 hours ÷ 64 ≈ 22.5 minutes).",
        "title": "IPv64.net Options"
      }
    }
  },
  "services": {
    "refresh": {
      "name": "Refresh IP Address",
      "description": "Manually refresh the IP address. Note: This consumes one of your 64 daily tokens (free account).",
      "fields": {
        "economy": {
          "name": "Economy Mode",
          "description": "Enable economy mode to update only when the IP changes."
        }
      }
    }
  }
}
}
